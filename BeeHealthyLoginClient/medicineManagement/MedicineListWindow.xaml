<Window x:Class="BeeHealthyLoginClient.medicineManagement.MedicineListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeeHealthyLoginClient.medicineManagement"
        mc:Ignorable="d"
        Title="MedicineListWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="btnBetoltes"
            Content="Betöltés"
            Click="btnBetoltes_Click"
            Width="100"
            Margin="10"/>
        <DataGrid x:Name="dgMedicines"
              Grid.Row="1"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              CanUserReorderColumns="False"
              CanUserResizeRows="False"
              CanUserResizeColumns="False"
              CanUserSortColumns="False"
              IsReadOnly="True"
              SelectionMode="Single"
              SelectionUnit="FullRow"
              Grid.RowSpan="2"
              >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Gyógyszernév"
                                Binding="{Binding GyogyszerNev}"/>
                <DataGridTextColumn Header="Kategória"
                                Binding="{Binding Kategoria}"/>
                <DataGridTextColumn Header="Adagolás"
                                Binding="{Binding Adagolas}"/>
                <DataGridTextColumn Header="Kezelési időpont"
                                Binding="{Binding KezelesiIdopont}"/>
                <DataGridTextColumn Header="Kezelés időtartama"
                                Binding="{Binding KezelesIdotartama}"/>
                <DataGridTextColumn Header="Megjegyzés"
                                Binding="{Binding Megjegyzes}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
