<Window x:Class="BeeHealthyLoginClient.medicineManagement.EditMedicineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeeHealthyLoginClient.medicineManagement"
        mc:Ignorable="d"
        Title="EditMedicineWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Button x:Name="btnBetoltes"
                Content="Betöltés"
                Click="btnBetoltes_Click"
                Width="100"
                Margin="10"/>

        <DataGrid x:Name="dgMedicines"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeRows="False"
                  CanUserResizeColumns="False"
                  CanUserSortColumns="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Gyógyszer Név"
                                    Binding="{Binding GyogyszerNev}"/>
                <DataGridTextColumn Header="Kategória"
                                    Binding="{Binding Kategoria}"/>
                <DataGridTextColumn Header="Gyártó ID"
                                    Binding="{Binding GyartoId}"/>
                <DataGridTextColumn Header="Megjegyzés"
                                    Binding="{Binding Megjegyzes}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Módosítás" Margin="5" Padding="10" Click="Modositas_Click"/>
            <Button Content="Törlés" Margin="5" Padding="10" Click="Torles_Click"/>
            <Button Content="Mégse" Margin="5" Padding="10" Click="Megse_Click"/>
        </StackPanel>
    </Grid>
</Window>
